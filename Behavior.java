public enum Behavior { // This class stores the behavior levels for the AI. It has 3 modes which are different values of the enum.
  RANDOM,              // Behave randomly
  EASY,                // Behave in a way that performs worse than random
  HARD;                // Behave in a way that performs better than random
}                      //